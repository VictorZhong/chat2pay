VITE_API_MODE=mock
VITE_BACKEND_PROXY_TARGET=http://localhost:8080
# Optional when you want the browser to call the backend directly instead of using the Vite proxy.
# VITE_BACKEND_API_BASE_URL=http://localhost:8080
VITE_MOCK_API_LATENCY_MS=220
VITE_MOCK_INTERACTION_DELAY_MS=3000
