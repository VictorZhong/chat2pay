This directory is reserved for OpenAPI-generated types.

During the initial scaffold phase, the app uses handwritten contract-compatible
types from `src/shared/api/contracts.ts`. Run `npm run codegen:types` later to
replace this placeholder with generated output from `../api-contract/chat2pay-api.yaml`.
